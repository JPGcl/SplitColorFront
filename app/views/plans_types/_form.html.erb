<%= form_with(model: plans_type) do |form| %>
  <% if plans_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(plans_type.errors.count, "error") %> prohibited this plans_type from being saved:</h2>

      <ul>
        <% plans_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :plan_description, style: "display: block" %>
    <%= form.text_field :plan_description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
