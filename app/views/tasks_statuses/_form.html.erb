<%= form_with(model: tasks_status) do |form| %>
  <% if tasks_status.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tasks_status.errors.count, "error") %> prohibited this tasks_status from being saved:</h2>

      <ul>
        <% tasks_status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :task_status_description, style: "display: block" %>
    <%= form.text_field :task_status_description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
