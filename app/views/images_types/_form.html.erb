<%= form_with(model: images_type) do |form| %>
  <% if images_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(images_type.errors.count, "error") %> prohibited this images_type from being saved:</h2>

      <ul>
        <% images_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :image_type_description, style: "display: block" %>
    <%= form.text_field :image_type_description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
